import { Theme, createStyles } from "@material-ui/core/styles";

const TopFeaturedStyle = (theme: Theme) =>
  createStyles({
    customSelect:{
      fontSize:"1.5rem",
      paddingLeft:"2px",
      lineHeight:1.4,
    }
  });

export default TopFeaturedStyle;
